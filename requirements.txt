# --- Core Backend ---
fastapi==0.129.0
uvicorn==0.40.0
python-dotenv==1.2.2
requests==2.33.0

# --- Image Processing ---
# CRITICAL: use headless variant — opencv-python pulls Qt/GUI libs (~200MB RAM hit on import)
pillow==12.1.1
numpy==1.26.4
opencv-python-headless==4.10.0.84

# --- ML (CPU ONLY - Railway safe) ---
# Must come BEFORE segmentation-models-pytorch so pip resolves the CPU index
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

segmentation-models-pytorch==0.3.3

# --- Albumentations (pin to avoid breaking API changes) ---
albumentations==1.3.1

# --- TigerGraph (optional — pipeline falls back gracefully if unavailable) ---
pyTigerGraph==1.8.0

# --- Diagnostics (lightweight, helps debug OOM on Railway) ---
psutil==5.9.8